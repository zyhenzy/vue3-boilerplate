// import { dispatchCloseRootSide } from '@/helper'
// import { KEEP_SIDE_NAMES } from '@/config/config.theme'
// import { includes } from 'lodash-es'
import type { Router } from 'vue-router'

const setupTheme = (router: Router) => {
  // router.beforeEach(to => {
  //   const { name } = to
  //
  //   if (!includes(KEEP_SIDE_NAMES, name)) {
  //     dispatchCloseRootSide()
  //   }
  // })
}

export default setupTheme
